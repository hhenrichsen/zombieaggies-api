html
    head
        title ZombieAggies | Login
    body
        h1 Login
        form(action="/auth/login" method="post")
            p
                label Username:
                    input(type="text" name="username")
            p
                label Password:
                    input(type="password" name="password")
            p
                button(type="submit") Login
        p: a(href="/auth/register") Register